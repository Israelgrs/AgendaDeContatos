<div class="basic-grey">
<p id="notice"><%= notice %></p>

<h1>Kinds</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Descrição</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @kinds.each do |kind| %>
      <tr >
        <td><%= kind.description %></td>
        <td><%= link_to 'Editar', edit_kind_path(kind) %></td>
        <td><%= link_to 'Excluir', kind, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo tipo', new_kind_path, class:"btn btn-primary" %>
<%= link_to 'Tela inicial', home_index_path, class:"btn btn-success" %>
</div>